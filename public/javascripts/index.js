/**
 * Module dependencies
 */

var App = require('poe-ui');

/**
 * Expose the app
 */

var app = module.exports = App('hoverfx');

/**
 * Load plugins here with the 'use' method:
 *
 *   app.use(require('modal'));
 *
 */
